{
    "license": "MIT",
    "scripts": {
        "start": "concurrently \"yarn start:server\" \"yarn start:client\"",
        "start:server": "cd node-server && yarn && yarn dev",
        "start:client": "cd react-client && yarn && yarn dev"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
